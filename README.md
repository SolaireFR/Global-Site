# Commandes utiles
```bash
# SPRING
docker exec -it ubuntu-spring-1 bash

cd ~/Projects/Global-Site/

mvn spring-boot:run

# MYSQL
docker exec -it mysql_server bash

mysql -u root -proot

USE global_site_db;
```